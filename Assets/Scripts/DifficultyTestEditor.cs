using System.Collections;
using System.Collections.Generic;
using UnityEditor;
using UnityEngine;
/*
public class DifficultyTestEditor : EditorWindow
{

    private bool makeScreenshot = true;
    private int numberOfPlayouts = 100;

    private DifficultyTest difficultyTest;

    DifficultyTestEditor()
    {
        difficultyTest = new DifficultyTest();
    }

    [MenuItem("Planetoids/Test Level Difficulty")]
    public static void ShowWindow()
    {
        GetWindow<DifficultyTestEditor>(false, "Difficulty Test", true);
    }

    void OnGUI()
    {
        makeScreenshot = EditorGUILayout.Toggle("Make screenshoot", makeScreenshot);
        numberOfPlayouts = (int) EditorGUILayout.Slider("Number of playouts", numberOfPlayouts, 1, 10000);
        if (GUILayout.Button("Start test"))
        {
            // Pass parameters
            EditorApplication.EnterPlaymode();
        }
    }
}
*/